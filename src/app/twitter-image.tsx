// Same generator as opengraph-image.tsx — X falls back to og:image when this
// convention is absent, but exporting it explicitly keeps both cards honest.
export { default, size, contentType } from "./opengraph-image";
